# 주식 자동 매매 기획

**[Project Alpha Trader]** - 미국/한국 주식 자동매매 봇

**다중 증권사 API** + **다중 무료 LLM(Gemini, Grok, DeepSeek, Groq)** 합의 기반 자동 투자 시스템

---

# 📁 문서 1. [기획서] Project Alpha Trader & Stock Hunter

## 1. 프로젝트 개요

- **프로젝트 명:** Alpha Trader (미국/한국 주식 자동매매 봇)
- **목표:** 
    1. **미국 시장**: 빅테크 및 레버리지 ETF를 중심으로 추세 추종. **3배 ETF 제한 없음.**
    2. **한국 시장**: ETF + 개별 종목 발굴(가치주/수급주) 전략. **1000만원 미만 시 레버리지 ETF 제한.**
    3. **AI 기반**: 다중 LLM 합의 시스템으로 뉴스 및 수급 분석을 통해 유망 종목을 실시간으로 포착.
- **핵심 대상:**
    - **US**: TQQQ, SOXL, NVDA, MSFT, AAPL 등 빅테크 및 ETF (3배 레버리지 항상 가능)
    - **KR**: KODEX 레버리지, 개별 우량주 및 거래량 급등주 (KRW 1000만원 이상 시 레버리지)
    - **자동 포트폴리오 (Dynamic Portfolio)**: 미리 정의된 30여 개의 고품질 ETF/개별주 풀(K-방산, 가치업, AI·반도체, 빅테크 등) 중 **추세추종 복합 점수(위험조정 멀티 모멘텀 + 추세 확인)** 가 우수한 상위 N개를 매일 필터링하여 자동으로 교체/삭제 편입. (v3.0 부터 단순 20일 수익률 → 복합 점수로 고도화. 레버리지·개별주는 `trend_ok` 통과 시에만 편입하여 섹터 싸이클 자동 추종)
        - ⚠️ 저장 포맷 규약: US 종목은 `{ "symbol": ..., "exchange": "NAS"|"AMS", "weight": ... }` 필수. `exchange` 누락 시 메인 루프에서 `KeyError` 로 미국 세션이 통째로 중단되므로, `modules/portfolio_manager.py` 가 거래소 매핑 테이블로 항상 채워 넣는다. 과거에 `exchange` 가 빠진 채 저장되어 수 주간 US 거래가 멈춘 사고가 있었음 (2026-05-26 핫픽스).
        - ⚠️ 회전 정상성: 재생성 호출 인자 불일치(`max_kr=10`)와 `KR_ETF_CODES.update(dict)` 의 `unhashable` 에러로 **회전이 수 주간 멈춰 있던 사고**가 있었음 (2026-06-24 v3.0 핫픽스). 부팅 시 1회 즉시 갱신 + 매일 08:00/23:10 KST 재생성으로 신선도 보장.
- **운영 시간:**
    - 한국장: 09:00 ~ 15:30
    - 미국장: 23:30 ~ 06:00
- **수익 모델:** 사용자 투자 무료, 광고 수익 기반

## 2. 투자 전략 (The Strategy)

### 2.0. 참고 문서 반영 개선안 (daily_stock_analysis 스타일)

레퍼런스 문서의 운영 방식(설정 중심, 요약 중심, 주기 점검)을 반영해 다음 원칙으로 개선합니다.

1. 설정 중심 정책화
- AI 합의 규칙을 코드 하드코딩이 아닌 설정(`user_config.json`)으로 제어
- 운영 환경마다 보수/공격 성향을 정책값으로 즉시 조정 가능

2. 의사결정 요약 표준화
- 대시보드 개요에 "오늘의 의사결정 요약(1문장)" 고정 노출
- 운영자가 장 상태/최근 주문/신호 강도를 즉시 파악하도록 단일 결론 제공

3. 주기 검증 루프 강화
- 주간 백테스트 자동 생성(일요일 07:00 KST)
- 실거래 히스토리 기반 성과/리스크 지표를 정기 점검

### 2.0.1. AI 합의 정책 설정 스키마

`user_config.json` 예시:

```json
{
    "llm_consensus": {
        "crash_veto": true,
        "min_successful_llms": 2,
        "required_buy_ratio": 0.6,
        "unknown_fallback_hold": true,
        "tie_breaker": "persona"
    }
}
```

| 키 | 기본값 | 의미 |
|----|--------|------|
| `crash_veto` | `true` | LLM 중 1개라도 CRASH면 매수 차단 |
| `min_successful_llms` | `1` | 합의 유효 판정 최소 응답 수(쿼럼) |
| `required_buy_ratio` | `0.5` | 매수 승인에 필요한 찬성 비율 |
| `unknown_fallback_hold` | `true` | 전체 실패 시 보수적 관망 여부 |
| `tie_breaker` | `persona` | 임계 동률 시 처리(`persona`/`buy`/`hold`) |

### 2.1. 시장별 차별화 전략

> **v5.0 갱신**: "한국장과 미국장은 특성이 다르다"는 원칙을 코드 레벨까지 반영.
> 같은 `aggressive_dca` 코드 경로를 쓰더라도 AI 판단 성향과 추세 확인 강도를
> 시장별로 다르게 적용한다 (`user_config.json`의 `market_settings.us` / `.kr`).

| 항목 | 🇺🇸 US | 🇰🇷 KR |
|------|--------|--------|
| AI 페르소나 | `neutral` | `conservative` (국내 개별 이슈발 변동성이 더 커서 더 보수적으로) |
| 추세 확인 | 20MA(1x) 또는 10MA(레버리지) 상회 | 20MA **AND** 5MA 동시 상회 — 더 엄격한 확인 |
| 섹터 뉴스 조회 언어 | 영문 (Google News, `hl=en-US`) | 국문 (Google News, `hl=ko`) |
| 레버리지 | 3배 ETF 상시 허용 | 예탁금 1,000만원 이상만 2배 ETF 허용 |

#### 🇺🇸 US 시장 전략
- **3배 레버리지 ETF 항상 사용 가능** (TQQQ, SOXL, TECL 등)
- 미국은 레버리지 ETF 투자에 예탁금 제한이 없음
- 대형주 + 레버리지 ETF 혼합 포트폴리오
- VBO(변동성 돌파) + DCA(분할매수) 하이브리드
- AI 판단은 `neutral` 페르소나 — 명백한 하락추세/악재 국면에는 매수를 자제하되
  사소한 조정에는 흔들리지 않음

#### 🇰🇷 KR 시장 전략
- **예탁금 1,000만원 미만**: 1배수 ETF + 개별 우량주만 거래
- **예탁금 1,000만원 이상**: 2배 레버리지 ETF 추가 가능
- 스캐너 기반 동적 종목 발굴 (단, 2026-07-07부로 개별주 신규 편입은 잠정 중단 — v3.1 참조)
- **개선된 리스크 관리**: 개별 주식의 높은 변동성 감안
- AI 판단은 `conservative` 페르소나 + 20MA·5MA 동시 확인 — 국내 수급/이슈 변동성이
  더 크다는 전제로 미국보다 한 단계 더 보수적으로 운용

### 전략 모드 선택

| 모드 | 설명 | 적합한 시장/대상 |
|------|------|------------|
| **VBO (변동성 돌파)** | 목표가 돌파 시 매수, 장 마감 매도 | 지수 ETF, 대형주 |
| **Scanner (종목 발굴)** | 거래량 급등, 수급 상위 종목 실시간 포착 | 한국 개별주, 급등주 |
| **DCA (분할매수)** | 매일 정해진 금액 분할 매수 | 장기 투자 (빅테크) |

### Step 1. [Trend Filter & Discovery] 종목 선정 및 추세 판단

**"수급이 몰리고 추세가 살아있는 종목을 산다."**

- **Target List (기본):** 사전에 정의된 우량주 및 ETF
- **Scanner (동적):** 장중 거래량 급등(Volume Spike) 및 거래대금 상위(Blue Chip Surge) 종목을 실시간 포착
- **AI Check:** 다중 LLM 합의를 통해 시장 붕괴 조짐이 있는지 확인

### Step 2. [Entry Trigger] 진입 타점

#### VBO 모드 (변동성 돌파)
- **공식:** 매수 주문가 = $Today Open + (Yesterday Range \times K)$
    - $Yesterday Range = (Yesterday High - Yesterday Low)$
    - $K = 0.5$ (노이즈 필터링 상수)

#### DCA 모드 (분할매수)
- 매일 고정 금액 투자 (가용 자금의 5%)
- 장 시작 30분 후 자동 매수 (시가 안정화 후)

#### HYBRID 모드 (하이브리드)
- **추세 상승 (20MA 위):** VBO 전략 실행
- **추세 횡보/하락 (20MA 아래):** DCA 전략 실행 (소량)

### Step 3. [AI Multi-LLM Consensus Filter] 재료 검증 (진입 직전)

**"기술적 신호가 떴지만, 혹시 오늘 파월 의장이 금리 인상 발언을 했는가?"**

- **다중 LLM 합의 시스템:**
    - **Gemini** (Google): 빠른 뉴스 해석, 영어 역량 탁월
    - **Grok** (xAI): X(트위터) 실시간 감성 분석
    - **DeepSeek** (DeepSeek): 중국/아시아 시장 관점 분석
    - **Groq** (Groq): 초고속 추론 (Llama/Mixtral 호스팅), 보조 의견
- **합의 방식:**
    - 각 LLM이 독립적으로 `risk_level` (HIGH/LOW)과 `can_buy` (true/false) 판단
    - **다수결(Majority Vote):** 과반수 이상이 매수 추천 시 매수
    - 한 LLM이라도 "CRASH" 판정 시 매수 차단

- **[v5.0 신규] 섹터 특화 뉴스 veto (`modules/sector_news.py`):**
    - 배경: 위 "시장 전반" 합의만으로는 "시장은 멀쩡한데 특정 섹터만 무너지는" 상황
      (예: 2026-07 중국발 반도체 수출 규제)을 감지하지 못해, 관련 섹터 ETF/종목을
      뉴스를 무시한 채 계속 매수하는 사고가 발생했음.
    - 종목 → 섹터 매핑(반도체/나스닥테크/2차전지/코스피 전반 등)을 두고, 매수 직전
      해당 섹터에 국한된 뉴스를 Google News RSS로 별도 조회.
    - Gemini 단독으로 판별하되(4-LLM 합의는 API 쿼터 소진 우려로 섹터 단위에는
      미적용), **트레이딩 페르소나와 무관하게 항상 엄격/객관적으로 판단**하는 전용
      프롬프트(`check_sector_crash`) 사용 — aggressive 페르소나로도 이 게이트는
      완화되지 않음.
    - 섹터 단위로 30분 캐시하여 API 호출을 절약.
    - 반도체가 CRASH 판정을 받아도 나스닥테크/2차전지 등 다른 섹터의 매수 기회는
      그대로 유지됨 (전체 시장 차단과 다름 — "필요한 만큼만" 차단).

### Step 4. [Constraints & Fallback] 제약 사항 및 대응

**🇺🇸 US 시장: 3배 레버리지 ETF 제한 없음**
- 미국 시장에서는 3배 레버리지 ETF 투자에 별도의 예탁금 제한이 없음
- 따라서 US 시장은 항상 전체 ETF 목록(3x 포함)으로 거래

**🇰🇷 KR 시장: 예탁금 1,000만원 기준**
- **예탁금 1,000만원 미만:** 1배수 ETF + 개별 우량주만 거래
- **예탁금 1,000만원 이상:** 2배 레버리지 ETF 추가 가능
- 자동으로 예탁금을 확인하여 모드 전환

### Step 5. [Exit Rule] 청산 전략

#### 시장별 차별화된 리스크 관리 (v2.6 갱신)

| 항목 | US 시장 (1x) | US 레버리지 (2x/3x) | KR 시장 (개별주) | KR 시장 (ETF) |
|------|-------------|---------------------|------------------|---------------|
| **Stop Loss (base)** | -5.0% | **-7.5%/-10%** (보수적 배율) | **-7.0%** | -5.0% |
| **ATR Dynamic Stop** | base 와 `-2×ATR%` 중 느슨한 쪽 (상한 -10%) | 상한도 보수적 배율 (-15%/-20%) | 동일 | 동일 |
| **Breakeven Trigger** | **+3.0%** → floor `+0.2%` | **+6%/+9%** (풀 배율) | **+4.0%** → floor `+0.2%` | +3.0% |
| **Partial TP (1차 익절)** | Trailing 활성가 도달 시 50% 청산 | 동일 (활성가 자체가 풀 배율) | 동일 | 동일 |
| **Trailing Stop 활성** | +5.0% | **+10%/+15%** (풀 배율) | **+7.0%** | +5.0% |
| **Trailing Stop 하락** | -3.0% | **-4.5%/-6%** (보수적 배율) | **-4.0%** | -3.0% |
| **Gap Down 차단** | -5.0% | -5.0% | **-6.0%** | -5.0% |
| **Portfolio Drawdown 차단** | -7.0% | -7.0% | -7.0% | -7.0% |
| **Correlation Cap** | 그룹당 동시 보유 ≤ 2 | 동일 | 동일 | 동일 |
| **Losing Streak Throttle** | 손절 3건 OR 누적 -3% → 신규 매수 중단 | 동일 | 동일 | 동일 |

> **v2.6 핵심 변경 — 비대칭 스케일링**: 레버리지 ETF(TQQQ 3x, SOXL 3x, TECL 3x, FNGU 3x, UPRO 3x, NVDL 2x)는
> 기초지수 대비 N배 가격이 움직이므로, 수익실현·손절·breakeven 임계값을 스케일링한다.
> 
> **상승(수익실현)**: 풀 배율(N배) 적용 → 기초지수 기준 동일 판정, 수익 극대화
> - 예: SOXL trailing 활성 = +15% (= SOXX +5%), breakeven = +9% (= SOXX +3%)
> 
> **하락(손절/방어)**: 보수적 배율 `(1+N)/2` 적용 → 복리 손실 조기 차단
> - 이유: 레버리지 ETF 하락은 volatility decay로 복리 손실이 가속됨
>   (3x ETF -15% → 회복에 +17.6% 필요 vs 1x -5% → +5.3%만 필요)
> - 예: SOXL stop loss = -10% (3x 보수적), trailing drop = -6% (3x 보수적)
> - NVDL stop loss = -7.5% (2x 보수적), trailing drop = -4.5% (2x 보수적)

#### 1. Stop Loss (손절매)
- **US/ETF:** -5.0% 시 매도 발동 → `safe_sell()` (시장가 3회 → 지정가 fallback 2회)
- **KR 개별주:** -7.0% 시 매도 발동 (단기 변동성 흡수)
- 매도 실패 시 `STOP_LOSS_MAX_RETRY_CYCLES=5` 사이클 한도 도달 시 강제 정리 (스팸 방지)

#### 2. Trailing Stop (트레일링 스탑)
- **US/ETF:** +5.0% 초과 후 고점 대비 -3.0% 하락 시 매도
- **KR 개별주:** +7.0% 초과 후 고점 대비 -4.0% 하락 시 매도

#### 3. Time Cut (종가 청산)
- 장 마감 10분 전 보유 종목 시장가 매도
- DCA/Swing 전략은 종가 청산 예외 (장기 보유)

#### 4. Gap Down Defense (갭다운 방어)
- 전일 종가 대비 급락 시 매수 차단 — 단 **변동성 반등 트리거** 충족 시 50% 수량으로 역방향 매수

#### 5. Portfolio Circuit Breaker
- 전체 포트폴리오 평가손실 -7.0% 이상 시 신규 매수 중단

#### 6. ⚡ Oversold Rebound Buy (변동성 반등 매수, v2.3 신규)
- 누적 하락/갭다운 ≥5% + 당일 저점 대비 반등 ≥1% + 현재가 ≥ 5MA 충족 시
- 매수 차단 가드를 무효화하고 **50% 수량으로 역방향 매수** (세션당 1회)
- AI veto 우회 (오버솔드 구간 진입 기회 확보)

#### 7. 🎯 Partial Take-Profit (1차 부분 익절, v2.4 신규)
- Trailing 활성가(+5% / +7%) 도달 시 보유 수량의 **50%**를 먼저 시장가 청산
- 잔여 수량은 기존 Trailing Stop 으로 추적 → "수익 일부 락-인 + 상승 추적" 비대칭 전략
- 최소 청산 수량 `PARTIAL_TP_MIN_QTY=2` 미만이면 skip (전량 trailing 유지)

#### 8. 🛡️ Breakeven Stop (본전 스탑, v2.5 신규)
- 고점이 **+3% (US/ETF) / +4% (KR 개별주)** 도달 시 본전 스탑 활성화
- 손절선이 매수가 +**0.2% 버퍼** 위로 끌어올려져, 이익을 보던 포지션이 손실로 전환되는 것을 1차 방어
- 일반 Stop Loss(-5%/-7%) 보다 우선하며, 이미 활성화된 trailing 보다 빠르게 발동

#### 9. 🔗 Correlation Cap (상관 그룹 한도, v2.5 신규)
- 동일 섹터/지수 추종 그룹(예: TQQQ·TECL·FNGU·NVDL·SOXL·QQQ·NVDA, 005930·000660)에서 **동시 보유 최대 2종목**
- 같은 시장 사건에 의한 동조 손실을 구조적으로 방지
- `correlation_groups` / `correlation_max_per_group` 으로 설정 가능

#### 10. 🛑 Losing Streak Throttle (일일 손실 회로차단, v2.5 신규)
- 하루 손절/breakeven 청산 **3건 이상** 누적 또는 일일 실현 PnL **-3% 이하** 시 신규 매수 즉시 중단
- KST 자정 기준 자동 리셋 → 다음날 정상 재개
- "심리적 손실 → 추격 매수 악순환" 차단

#### 11. 📏 ATR 동적 Stop (v2.5 신규)
- 14일 ATR 을 종목별로 산정하여 기본 손절폭을 보다 **느슨한 쪽**으로 동적 조정
- 노이즈 컷 회피 (변동성 큰 종목이 기본 -5% 에 너무 자주 걸리는 문제 완화)
- 안전 상한 `ATR_STOP_MAX_PCT = -10%` 로 과도 확장 방지 (레버리지 ETF는 배율 적용: 3x → -30%)

#### 12. ⚙️ Walk-Forward 비용 모델 (v2.5 신규)
- 시뮬레이터에 `slippage_pct=0.05%`, `fee_pct=0.015%(양방향)` 적용
- 백테스트 수익이 실거래 체결가/수수료 차감 후에도 유의미한지 검증 가능

#### 13. ⚖️ 레버리지 ETF 비대칭 스케일링 (v2.6 신규)
- **문제**: 레버리지 ETF(TQQQ 3x, SOXL 3x 등)는 기초지수 대비 N배 가격이 움직이므로, 1x ETF와 동일한 수익실현 기준(+5% trailing 등)을 적용하면 기초지수 기준으로 매우 낮은 수준(+1.67%)에서 조기 매도됨
- **해결**: `LEVERAGED_ETF_FACTOR` 매핑으로 종목별 배율을 정의하고, **상승/하락 비대칭 스케일링** 적용
- **대상**: TQQQ(3x), SOXL(3x), TECL(3x), FNGU(3x), UPRO(3x), NVDL(2x)
- **비대칭 원리**:
  - 레버리지 ETF 하락은 volatility decay로 복리 손실이 가속됨 (3x -15% → 회복에 +17.6% 필요)
  - 따라서 **상승은 풀 배율**(수익 극대화), **하락은 보수적 배율 `(1+N)/2`**(자본 보호)
- **상승(풀 배율 N) 적용 항목**:
  - Trailing 활성: +5% × 3 = **+15%** (3x), +5% × 2 = **+10%** (2x)
  - Breakeven: +3% × 3 = **+9%** (3x), +3% × 2 = **+6%** (2x)
- **하락(보수적 배율 (1+N)/2) 적용 항목**:
  - Stop Loss: -5% × 2.0 = **-10%** (3x), -5% × 1.5 = **-7.5%** (2x)
  - Trailing 하락: -3% × 2.0 = **-6%** (3x), -3% × 1.5 = **-4.5%** (2x)
  - ATR Stop 상한: -10% × 2.0 = **-20%** (3x), -10% × 1.5 = **-15%** (2x)

---

## 3. 기술 스택

- **Broker:** 다중 증권사 지원 (추상 인터페이스)
    - **한국투자증권 (KIS):** REST API - 현재 구현 완료
    - **키움증권:** OpenAPI+ (추후 지원 예정)
    - **NH투자증권:** QV API (추후 지원 예정)
    - **Alpaca:** US 주식 전용 (추후 지원 예정)
- **AI Engine:** 다중 LLM 합의 시스템
    - Google Gemini 1.5 Flash (무료)
    - xAI Grok (무료)
    - DeepSeek (무료)
    - Groq - Llama/Mixtral (무료)
- **Language:** Python 3.10+
- **Dashboard:** FastAPI + Jinja2 (primary), Streamlit dashboard.py (legacy)
- **Server:** Oracle Cloud Free Tier
- **Notification:** Telegram Bot
- **인증:** Google OAuth 2.0 (Streamlit 대시보드 기준)
- **수익:** Google AdSense

---

## 4. 시스템 아키텍처

### 다중 증권사 인터페이스

```
BrokerInterface (ABC)
├── get_current_price(ticker, exchange)
├── get_daily_ohlc(ticker, exchange)
├── buy_market_order(ticker, qty, exchange)
├── sell_market_order(ticker, qty, exchange)
├── get_balance()
└── get_foreign_balance()

구현체:
├── KISBroker (한국투자증권)
├── KiwoomBroker (키움증권) [예정]
├── NHBroker (NH투자증권) [예정]
└── AlpacaBroker (Alpaca) [예정]
```

### 다중 LLM 합의 시스템

```
MultiLLMAnalyst
├── GeminiAnalyst (Google)
├── GrokAnalyst (xAI)
├── DeepSeekAnalyst (DeepSeek)
└── GroqAnalyst (Groq/Llama)

합의 로직:
1. 각 LLM에 동일한 뉴스 + 프롬프트 전달
2. 각 LLM이 독립적으로 risk_level + can_buy 판단
3. 다수결 투표 (Majority Vote)
4. CRASH 판정이 하나라도 있으면 매수 차단

### 운영 결론(Decision Brief) 생성 레이어

```
Dashboard Payload Builder
├── 상태(status.marketStatus)
├── 최근 주문(activity.lastOrder)
├── 최상위 신호(signals[0])
└── 1문장 결론(status.decisionBrief) 생성
```

결론 예시:
- "관망 권장: 시장 마감 상태입니다."
- "주의: 최근 주문 실패가 감지되었습니다."
- "공격 관찰: SOXL 신호 강도가 높습니다."
```

## 5. 폴더 구조

```
AlphaTrader/
├── run_bot.py              # 메인 실행 파일 (스케줄러)
├── config.py               # API Key 관리
├── user_config.json        # 사용자 설정
├── dashboard.py            # 레거시 Streamlit 대시보드
├── strategies/
│   ├── volatility_breakout.py
│   └── technical.py
├── modules/
│   ├── broker_base.py      # 증권사 추상 인터페이스
│   ├── kis_api.py          # KIS 해외주식 API
│   ├── kis_domestic.py     # KIS 국내주식 API
│   ├── multi_llm.py        # 다중 LLM 합의 엔진
│   ├── gemini_analyst.py   # Gemini 분석기
│   ├── grok_analyst.py     # Grok 분석기
│   ├── deepseek_analyst.py # DeepSeek 분석기
│   ├── groq_analyst.py     # Groq 분석기
│   ├── telegram_notifier.py
│   ├── account_manager.py
│   ├── market_scanner.py
│   └── logger.py
├── web/
│   ├── app.py              # FastAPI 대시보드
│   └── templates/
└── database/
```

## 6. 환경 변수 (.env)

```
# 증권사 API
KIS_APP_KEY=
KIS_APP_SECRET=
KIS_CANO=
KIS_ACNT_PRDT_CD=01
KIS_MOCK=False

# AI LLM API Keys
GEMINI_API_KEY=         # Google AI Studio
GROK_API_KEY=           # xAI Grok (https://console.x.ai)
DEEPSEEK_API_KEY=       # DeepSeek (https://platform.deepseek.com)
GROQ_API_KEY=           # Groq (https://console.groq.com)

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
```

## 6.1. 운영 스케줄 정책 (KST)

| 작업 | 주기 |
|------|------|
| 장중 봇 프로세스 감시 | 30초 |
| 장외 봇 감시 대기 | 5분 |
| 일일 리포트 | 매일 16:00 |
| 주간 백테스트 | 매주 일요일 07:00 |

## 7. 지원 증권사 목록

| 증권사 | API 유형 | 국내 | 해외 | 상태 |
|--------|----------|------|------|------|
| **한국투자증권** | REST API | ✅ | ✅ | 구현 완료 |
| **키움증권** | OpenAPI+ | ✅ | ✅ | 예정 |
| **NH투자증권** | QV API | ✅ | ✅ | 예정 |
| **Alpaca** | REST API | ❌ | ✅(US) | 예정 |

## 8. 수익 구조

- **사용자 투자:** 완전 무료
- **수익원:** 광고 (Google AdSense)
- **광고 영역:** 대시보드 상단 배너, 사이드바
- **추후:** 프리미엄 기능, 증권사 리퍼럴

---

# 📁 문서 2. 버전 이력

## v5.0 (2026-07-28) — 뉴스 게이트 전면 복원 + 섹터 특화 veto 신규 도입

### 🚨 개편 배경

v4.0(2026-06-24)이 "무조건 돈을 벌어야 한다 → 매수는 무조건 실행한다"는 원칙 하에
`aggressive_dca` 전략의 AI 뉴스 체크(`skip_ai_check`)와 추세 필터(`skip_trend_filter`)를
전부 비활성화했다. 그 직후인 2026-07, 중국발 반도체 수출 규제 뉴스로 전세계 반도체
업종이 붕괴했는데도 봇은 이를 전혀 인지하지 못한 채 SOXL(3배 반도체 레버리지)·SMH를
매일 기계적으로 물타기 매수하며 몇 주간 큰 손실을 냈다. 동시에 "미국장 거래 자체가
안 된다"는 별도 증상도 보고되었다 — 코드 감사 결과 US 잔고 조회(`get_foreign_balance`)
실패가 예외 없이 조용히 삼켜지는 지점(`available_cash`가 0으로 고정)이 발견되었으며,
이 경우 세션 내내 모든 US 티커가 "자금 부족"으로 알림 없이 차단된다. 과거
2026-05/06에도 동일한 "조용히 삼켜지는 예외" 패턴으로 US 세션이 몇 주간 죽어있던
전례가 있어, 유력한 원인으로 판단했다.

### 🎯 핵심 개편

1. **매수 게이트 전면 복원** — `aggressive_dca`의 `skip_ai_check`/`skip_trend_filter`/
   `skip_correlation_check` 기본값을 전부 원복(게이트 ON). "공격적(aggressive)"이라는
   이름은 이제 "매수 조건 없음"이 아니라 "매수 주기가 잦음(자주 분할매수)"만을 의미.
2. **[신규] 섹터 특화 뉴스 veto** — `modules/sector_news.py`(종목→섹터 매핑),
   `GeminiAnalyst.check_sector_crash`(페르소나 무관 엄격 판별),
   `MultiLLMAnalyst.check_sector_sentiment`(30분 캐시)를 신규 추가. 매수 직전 종목이
   속한 섹터(반도체/나스닥테크/2차전지/코스피 전반 등) 전용 뉴스를 Google News RSS로
   조회해, "시장 전체"가 아닌 "이 섹터"만 국한하여 크래시를 판별한다. 관련 없는
   섹터의 매수 기회는 그대로 유지된다.
3. **포트폴리오 드로다운 서킷브레이커 실효화** — 기존에는 드로다운 초과 시 Telegram
   알림만 발송하고 신규 매수는 계속 실행되는 버그가 있었다. v5.0부터는 실제로
   진입 루프 전에 평가하여 신규 매수(및 물타기)를 차단하는 플래그로 사용한다.
4. **US 잔고 조회 실패 알림** — `get_foreign_balance()` 응답에 `deposit` 필드가
   없거나 예외가 발생하면 로그뿐 아니라 Telegram으로도 즉시 알림.
5. **세션 매수 0건 워치독** — 후보 종목이 있었는데 세션 내내 실제 매수가 한 건도
   없으면, 차단 사유(`skipped_buy_reasons`)와 함께 세션 종료 시 경고 발송.
6. **시장별 AI persona 차등화** — `market_settings.us.persona = "neutral"`,
   `market_settings.kr.persona = "conservative"`. 또한 KR은 추세 확인 시 20MA와
   5MA를 모두 확인(더 엄격), US는 레버리지 ETF에 한해 10MA만 확인.
7. **v4.0에서 defang된 안전장치 재활성화** — `correlation_cap_enabled`,
   `losing_streak_enabled`, `llm_consensus.crash_veto`(+`required_buy_ratio` 0.0→0.5)
   전부 복원. 물타기 트리거도 -2.0% → -3.0%로 소폭 보수화.

### 🧪 테스트

- `tests/test_v50_strategy.py` 신규 10건: 섹터 매핑(US/KR 반도체·나스닥테크 티커),
  게이트 기본값 회귀 방지, 섹터 캐시/페일세이프(뉴스 없음 시 안전 통과, 캐시 히트 시
  재호출 방지) 검증.
- 기존 테스트 전체(66건) 통과 확인 (KIS 실계좌 인증이 필요한 2건은 로컬 환경 특성상
  스킵 — 이 저장소에 실 API 키가 없어 발생하는 사전부터의 무관한 실패).

### ❌ v5.0에서 의도적으로 보류한 항목
- KR 개별 종목 재도입 (2026-07-07 v3.1에서 전면 중단한 상태 유지 — 반복된 변동성
  손실 이력을 감안해 이번 개편에서는 건드리지 않음)
- 섹터 veto의 4-LLM 합의 확장 (API 쿼터 부담 대비 이득이 낮다고 판단, Gemini 단독 유지)
- 새 증권사/브로커 연동

## v1.0 ~ v1.4
- 기본 VBO + DCA 전략, KIS API, Gemini AI, Streamlit 대시보드
- 타임존 버그, DCA 리스크 관리 수정

## v2.0 (2026.04.19)

### 🚨 CRITICAL
1. **US 3배 ETF 제한 해제** - US는 항상 3x ETF 거래 가능
2. **US 보유 종목 감지 수정** - `ovrs_pdno` 폴백 추가
3. **KR 개별주 손절 완화** - StopLoss -3% → -5%, TrailingStop +3% → +5%

### ⚠️ MODERATE
4. **다중 LLM 합의 시스템** - Gemini + Grok + DeepSeek + Groq
5. **다중 증권사 인터페이스** - BrokerInterface ABC 패턴
6. **Google OAuth 로그인** - 대시보드 소셜 로그인

### 📝 MINOR
7. KR 종목 선정 개선 (저변동성 우량주 중심)
8. 광고 수익 구조 설정

## v2.1 (2026.04.27)

### 🚀 US 레버리지 ETF DCA 매수 조건 완화

**문제점:**
- US 시장에서 3배 ETF 제한은 없지만, DCA 전략의 기술적 필터(20MA, 갭다운, 연속하락)에 의해 
  SOXL, NVDL 등 레버리지 ETF 매수가 자주 차단됨
- 레버리지 ETF는 변동성이 높아 일반 종목 기준 필터에 과도하게 걸림

**해결책:**
US 시장 레버리지 ETF(3X/2X)에 대해 DCA 매수 조건 완화:

| 항목 | 일반 종목 | US 레버리지 ETF |
|------|-----------|-----------------|
| **이동평균 기준** | 20MA | **10MA** (단기 추세) |
| **갭다운 임계치** | -3.0% | **-6.0%** (2배 완화) |
| **연속 하락 차단** | 2일, 3% 이상 | **3일, 5% 이상** (완화) |

**적용 대상:**
- NVDL (2x NVDA)
- SOXL (3x Semiconductor)
- TQQQ (3x QQQ)
- TECL (3x Technology)
- FNGU (3x FANG+)
- BITX (2x Bitcoin)
- CONL (2x Coinbase)

## v2.2 (2026.05.06)

### ✅ 참고 문서 반영 운영 개선
1. **설정 기반 LLM 합의 정책 도입**
    - `llm_consensus` 설정으로 쿼럼/매수비율/CRASH veto/동률처리 정책화

2. **대시보드 의사결정 1문장 요약 추가**
    - 개요 화면에 `status.decisionBrief` 노출
    - 장 상태/주문 실패/고강도 신호를 즉시 요약

3. **문서 체계 정비**
    - README와 기획서의 정책/운영 주기를 일치시켜 운영 혼선 최소화

## v2.5 (2026.05.24) — 멀티-레이어 리스크 가드 & 비용 모델

### 🆕 핵심 추가
1. **Breakeven Stop (본전 스탑)**
    - 고점 +3% (US/ETF) / +4% (KR 개별주) 도달 시 손절선을 매수가 +0.2% 위로 끌어올림
    - 이익 포지션의 손실 전환 차단 → 일반 Stop Loss 보다 우선 발동
    - `BREAKEVEN_ENABLED`, `BREAKEVEN_TRIGGER_PCT_US`, `BREAKEVEN_BUFFER_PCT`

2. **Correlation Cap (상관 그룹 한도)**
    - 동일 섹터/지수 그룹(예: TQQQ·TECL·FNGU·NVDL·SOXL, 005930·000660)에서 동시 보유 최대 2종목
    - 시장 동조 손실 구조적 방지
    - `correlation_groups` 가 `user_config.json` 에서 자유 정의 가능

3. **Losing Streak Throttle (일일 손실 회로차단)**
    - 일일 손절/breakeven 3건 이상 OR 누적 실현 PnL ≤ -3% 시 신규 매수 즉시 중단
    - KST 자정 기준 자동 리셋

4. **ATR-based Dynamic Stop**
    - 14일 ATR 로 종목별 변동성 반영한 손절폭을 보수적으로 동적 조정
    - 안전 상한 -10% 클램프

5. **Walk-Forward 비용 모델**
    - `slippage_pct=0.05%`, `fee_pct=0.015%(양방향)` 시뮬레이터 반영
    - 백테스트 수익이 실거래 비용 후에도 유의미한지 검증
    - `v2.5_us`, `v2.5_kr_stock` 신규 PARAM_SET

6. **Pullback Re-buy (스캐폴딩)**
    - 부분익절 이후 5MA 재터치 → 30% 수량 재진입 옵션 (v2.5.1 에서 본격 활성화 예정)
    - 설정 키만 선반영: `pullback_rebuy_enabled`, `pullback_rebuy_ratio`

### ♻️ 리팩토링
- **multi_llm 응답 캐시 (5분 TTL)**: 동일 뉴스 텍스트 반복 질의 시 LLM API 호출/쿼터 절약 (`_CONSENSUS_CACHE`)
- 리스크 관련 상수를 v2.5 섹션 주석 블록으로 그룹화 (`run_bot.py` L74-110)
- `effective_stop_loss_pct()`, `get_breakeven_trigger_pct()`, `is_correlation_capped()`, `is_losing_streak_pause()` 헬퍼 분리

### 🧪 테스트
- v2.5 신규 단위 테스트 11개 추가 (총 37 tests pass, 12.5s)
    - Breakeven (3 케이스): arm/no-arm/loss-conversion 방지
    - 슬리피지/수수료 차감 검증
    - ATR 계산 + dynamic stop 보수성
    - Correlation cap (under/at cap)
    - Losing streak (single/max-stops/cumulative-pnl)

### ❌ v2.5에서 의도적으로 보류한 항목 (현 단계 비목표)
- 머신러닝 기반 신호 (충분한 거래 데이터 누적 후 v3.x 검토)
- 새 LLM 모델 추가 (기존 4개 합의로 충분, 호출 비용 우선)
- 신규 매매 전략 (DCA + Swing + VBO 안정화 우선)
- 추가 증권사 (KIS API 신뢰도 검증 후 v3 단계)

## v2.3 (2026.05.23) — 수익 극대화 & 매도 안정화

### 🚨 CRITICAL — 손절매 매도 주문 실패 무한 알람 버그 수정

**문제점:**
- 005930, 000660 등 KR 개별주에서 손절매 발동 후 매도 주문이 반복적으로 실패하며 텔레그램 알람이 무한히 누적됨
- 원인:
  1. **Rule No.0 (재시도 루프)** 가 `data['buys']` (메모리 캐시) 만 사용 → 실제 KIS 잔고와 mismatch 시 영원히 실패
  2. **장 외 시간 매도 시도** — KR 정규장(09:00–15:30) 종료 후에도 시장가 주문 시도 → KIS 거부
  3. **시장가 단일 의존** — `ORD_DVSN=01` 거부 시 fallback 없음
  4. **에러 코드 비공개** — `msg_cd`/`rt_cd` 로깅 부재로 원인 추적 불가

**해결책 — `safe_sell()` 통합 매도 헬퍼 도입 (`run_bot.py`):**
1. **잔고 재조회** — 매 시도마다 `kis.get_holding_qty(ticker)` 로 실제 보유 수량 재확인. 0이면 `already_flat` 처리(외부 청산 완료 인식). 캐시 수량 > 실제 보유 시 실제 수량으로 클램프.
2. **시장 시간 가드** — `is_market_open_for(market)` 가드. 장 마감이면 `deferred` 반환하고 알람 보내지 않음.
3. **시장가(3회) → 지정가 fallback(2회)** — `sell_limit_order` (KR) 추가. 호가단위 자동 라운딩 적용 (`_tick_size`). 지정가는 현재가의 -0.5% 할인하여 즉시 체결 유도.
4. **재시도 한도** — `STOP_LOSS_MAX_RETRY_CYCLES=5` 도달 시 강제 `sold_sl` 처리 + 단 1회 운영자 알림 (스팸 방지).
5. **상세 로깅** — `rt_cd`, `msg_cd`, `msg1` 모두 로깅하여 KIS 거부 사유 즉시 식별 가능.

같은 헬퍼가 Stop Loss, Trailing Stop, DCA Stop Loss, Rule No.0 재시도 4곳 모두에서 일관되게 사용됨.

### 🚀 수익 극대화 — 매매 빈도 & 투자 비중 강화

**기존 문제:**
- 호황장에서도 보유 비중이 작아 수익 기회를 놓침
- `STOP_LOSS=-3%`, `TRAILING_DROP=1.5%` → 호황장 노이즈에 조기 청산 → 재진입 못함
- `GAP_DOWN=3%`, `연속하락=3%` → 큰 하락 후 반등 구간에서 매수가 차단되어 변동성 활용 실패

**파라미터 재조정 (`user_config.json`):**

| 항목 | v2.2 | v2.3 | 의도 |
|---|---|---|---|
| `dca_settings.daily_investment_pct` | 15% | **30%** | 일간 투자 비중 2배 확대 |
| `dca_settings.max_investment_usd` | $500 | **$1,500** | 종목당 매수 상한 3배 |
| `dca_settings.max_buys_per_session` | 3 | **5** | 세션 매수 횟수 67%↑ |
| `risk_management.stop_loss_pct` | -3.0% | **-5.0%** | 노이즈 흡수 → 조기 청산 방지 |
| `risk_management.trailing_stop_activation_pct` | 3.0% | **5.0%** | 충분한 익절 폭 확보 |
| `risk_management.trailing_stop_drop_pct` | 1.5% | **3.0%** | 고점 대비 변동 허용 폭 ↑ |
| `risk_management.gap_down_threshold_pct` | 3.0% | **5.0%** | 매수 차단 완화 |
| `risk_management.consecutive_decline_pct` | 3.0% | **5.0%** | 매수 차단 완화 |
| `risk_management.portfolio_drawdown_pct` | 5.0% | **7.0%** | 일시적 변동에 휘둘리지 않음 |
| `KR_STOCK_STOP_LOSS_PCT` | -5.0% | **-7.0%** | 개별주 변동성 추가 흡수 |
| `KR_STOCK_TRAILING_ACTIVATION` | 5.0% | **7.0%** | 익절 폭 확대 |
| `KR_STOCK_TRAILING_DROP` | 2.5% | **4.0%** | 조기 청산 방지 |
| `KR_STOCK_GAP_DOWN_THRESHOLD` | 4.0% | **6.0%** | 매수 차단 완화 |

### ⚡ 변동성 반등 매수 (Oversold Rebound Trigger)

**문제:** 큰 하락(갭다운 또는 연속하락 ≥5%) 후 당일 반등이 시작되어도 기존 가드가 무조건 매수를 차단하여 회복 구간 진입 기회를 놓침.

**해결:** `attempt_dca_buy()` 에서 차단 조건이 발동되더라도 아래 모든 조건을 만족하면 차단을 무효화하고 **50% 수량으로 역방향 매수**:
- 누적 하락 또는 갭다운 ≥ `REBOUND_DROP_THRESHOLD_PCT` (기본 5%)
- 당일 저점 대비 반등 ≥ `REBOUND_INTRADAY_BOUNCE_PCT` (기본 1%)
- 현재가가 5MA 이상 (단기 반전 신호)
- 세션 반등 매수 횟수 < `REBOUND_MAX_BUYS_PER_SESSION` (기본 1)
- 반등 매수 트리거 시 AI veto 우회 (오버솔드 진입 기회 확보)

### 🧪 모듈 테스트

새 파일 `tests/test_safe_sell.py` 추가:
- 외부 청산(`already_flat`) 시 매도 주문 미발생 검증
- 시장가 1회 성공 / 캐시 수량 > 실보유 → 실보유로 클램프
- 시장가 3회 실패 → 지정가 fallback 성공 / 두 단계 모두 실패 시 `failed` 반환
- 장 마감 시 `deferred` 반환 (알람 안 보냄)
- KRX 호가단위(`_tick_size`) 7개 가격대 검증

모든 7개 케이스 통과 (`python -m unittest tests.test_safe_sell`).

### 🛠️ KIS 국내 API 헬퍼 신설 (`modules/kis_domestic.py`)
- `get_holding_qty(ticker)` — 실시간 매도가능 수량 조회
- `sell_limit_order(ticker, qty, price)` — 지정가 매도 (호가단위 자동 라운딩)
- `_tick_size(price)` — KRX 호가단위 정적 헬퍼
- `sell_market_order` 응답 상세 로깅 (`msg_cd`, `msg1`)

해외 API(`modules/kis_api.py`)에도 동일 컨셉으로 `get_holding_qty()` 추가.



### ⚡ 미국 시장 실시간 웹소켓(WebSocket) 하이브리드 엔진 도입 (v2.6)

**문제:**
초고변동성 3배 레버리지 ETF(SOXL, TQQQ 등)의 경우 1초~5초 간격의 주기적인 REST API 폴링 방식만으로는 급격한 돌파(Breakout)나 실시간 변동에 민감하게 대응하기 어려움. 또한 빈번한 REST API 호출은 한국투자증권 Open API의 초당 레이트 리밋(초당 2~3회 제한)을 쉽게 촉발시켜 시스템 불안정을 초래함.

**해결:**
`websockets` 라이브러리를 활용해 실시간 US 시세 전송 프로토콜(`HDFSZC413000` 실전 / `H0STCNT0` 모의)을 연동한 실시간 가격 수신 엔진(`modules/kis_websocket.py`)을 상시 백그라운드 데몬 스레드로 기동.

1. **상세 설계 및 거래소 자동 접두사 분기**:
   - 미국 주식에 대해 고유한 실시간 요청 키 형식 제공.
   - NASDAQ 종목은 `DNAS{SYMBOL}`, AMEX 종목은 `DAMS{SYMBOL}`, NYSE 종목은 `DNYS{SYMBOL}` 형태로 정확하게 조립하여 등록 세션을 맺도록 동적 매핑 엔진 수립.
   - 실시간 시세 수집 수신 패킷에서 구분자(`^`)와 파싱 룰을 활용하여 가격 필드를 추출하고 Thread-safe하게 실시간 가격 딕셔너리(`WS_PRICES`)를 갱신.

2. **초저지연 하이브리드 감시 및 리밋 방어 정책 (Hybrid Polling-Streaming Fallback)**:
   - 감시 대상 종목들의 실시간 가격(`WS_PRICES`)을 최우선 평가.
   - **폴링 차단 최적화:** 실시간 웹소켓 가격이 설정한 변동성 돌파 타겟가(`target_price`) 미만일 경우 불필요한 KIS REST API 조회를 원천 차단하여 리밋 소모 0회로 고정.
   - **이중 트레이딩 안심 방어 (Double-Check Buy Verification):** 실시간 웹소켓 가격이 돌파가 이상에 도달한 순간에만, 최종 리스크 통제 및 급격한 오버슈팅 검증을 위해 즉각 KIS REST `get_quote`를 호출하여 최신 체결가 및 실시간 거래량(`tvol`)의 급증 기준(Spike)을 즉각 교차 조회한 후 안전하게 주문을 전송.
   - **Fail-safe 원칙:** 웹소켓 승인키 발급 지연이나 세션 끊김 발생 시 기존 룰 기반의 REST 폴링 방식으로 물 흐르듯(Seamless) 자동 복귀.

---

## v3.0 (2026.06.24) — 수익 극대화 전면 개정 (동적 회전 + 비대칭 청산)

> **개정 배경**: 운영 중 "① 포트폴리오가 너무 고정됨 ② 반도체 등 섹터 싸이클을 못 따라감
> ③ 상승 여력이 분명한데 조기 매도되고, 하락 여력이 분명한데 손절이 안 됨" 3가지 문제가 보고됨.
> 코드 분석 결과 **동적 포트폴리오 시스템이 버그로 완전히 멈춰 있었고**, 기획서에만 존재하던
> 레버리지 비대칭 청산(v2.6)이 **코드에 미구현** 상태였음. 이를 전면 수정.

### 🚨 CRITICAL — 동적 포트폴리오 회전 3중 버그 수정 (불만 ① 근본 원인)

1. **포트폴리오 재생성 호출 버그**
   - `update_dynamic_portfolio()` 가 `generate_and_save_portfolio(max_kr=10)` 로 **존재하지 않는 인자**를 전달 → 매일 `TypeError` 발생 → `except` 로 조용히 삼켜짐.
   - 결과: `database/portfolio_target.json` 이 **수 주간 갱신되지 않고 고정**(2026-06-04 이후 멈춤)됨.
   - 수정: `portfolio_selection` 설정 기반 올바른 인자(`max_kr_etf`, `max_us_3x` 등)로 호출. 부팅 시 1회 즉시 갱신 추가.

2. **동적 포트폴리오 로드 버그 (`unhashable type: dict`)**
   - `KR_ETF_CODES.update(kr_dyn)` 에서 `kr_dyn` 의 dict 항목(`{code,category,weight}`)을 set 에 직접 추가 → `TypeError` → **동적 포트폴리오 로드가 통째로 중단**되고 하드코딩 목록으로 폴백.
   - 수정: 코드 문자열만 안전 추출하여 set 갱신.

3. **비중(weight) 미반영 버그**
   - `portfolio_target.json` 에 저장된 카테고리 비중(코어 ETF=1.0, 새틀라이트/개별주<1.0)이 `calculate_order_quantity` / `calculate_dca_quantity` 에서 **사용되지 않고 무시**됨.
   - 수정: 두 함수에 `weight` 파라미터 추가 + 매수 시점에 `ticker_weights` 로 실제 반영. 저비중 새틀라이트는 소액 매수.

### 🎯 종목 선정 고도화 — 추세추종 복합 점수 (불만 ② 해결)

기존: **단순 20일 수익률**만으로 정렬 → 싸이클 선도/추격 구분 불가.

신규: `PortfolioManager.get_composite_score()` — **위험조정 멀티 lookback 모멘텀 + 추세 확인**.

| 요소 | 산식 | 의도 |
|------|------|------|
| 블렌디드 모멘텀 | `0.2·R5 + 0.5·R20 + 0.3·R60` | 단기 노이즈 배제, 중기 추세 강조 |
| 위험조정 | `blended / ATR%` | 변동성 대비 효율 → **매끄럽게 오르는 싸이클 선도주** 우대 |
| 추세 보너스 | 가격>20MA(+0.3), 20MA≥60MA(+0.2) | 추세 정렬 종목 가점 |
| 추세 이탈 강등 | 가격<20MA → ×0.5 | 약세 종목 자동 후순위 |

- **레버리지(3X) ETF·개별주는 `trend_ok`(가격>20MA) 통과 시에만 편입** → 하락추세 레버리지 ETF의 변동성 decay 손실을 구조적으로 차단. 반도체가 선도하면 SOXL/SMH 가 자연스럽게 상위로 회전 편입, 약세 전환 시 자동 제외.

### ⚖️ 레버리지 ETF 비대칭 청산 — 기획→코드 구현 (불만 ③ 해결)

v2.6 에 설계만 있고 미구현이던 **비대칭 스케일링을 실제 코드로 구현**(`run_bot.py`).

- `LEVERAGED_ETF_FACTOR` 매핑(TQQQ/SOXL/TECL/FNGU/UPRO=3, NVDL=2 …, `user_config` override 가능)
- **상승(수익실현·활성·본전 트리거) = 풀 배율 N** → winners 가 추세를 충분히 추적 (조기 매도 방지)
  - 예: SOXL(3x) Trailing 활성 +5%→**+15%**, Breakeven 트리거 +3%→**+9%**
- **하락(손절·Trailing drop·ATR cap) = 보수적 배율 (1+N)/2** → 복리 decay 손실 조기 차단
  - 예: SOXL 손절 -6%→**-12%**, Trailing drop 3.5%→**7%**
- 효과: "3x ETF가 +3% 에 본전청산되어 상승을 놓치던" 조기 매도 문제 해소.

### 📉 추세붕괴 손절 (Trend-Collapse Exit) — 신규 (불만 ③ 해결)

보유 종목이 **명확히 20MA 를 하회 + 손실 구간**이면, %손절선 미도달이라도 **즉시 청산**.

- 게이트: `pnl ≤ trend_exit_min_loss_pct(-1%)` 이고 `현재가 < 20MA × (1 − buffer/배율)`
- 레버리지 배율이 클수록 버퍼를 좁혀(`buffer / factor`) **더 민감하게** 손절 (3x 는 20MA 하회 즉시, 1x 는 1% 여유)
- 이미 수익 구간(본전 아밍)을 경험한 포지션은 Breakeven/Trailing 에 위임 → 위너 churn 방지
- 효과: "하락 여력이 분명한데도 매도가 안 되던" losers 방치 문제 해소. ATR 동적손절이 손절선을 느슨하게 풀어도, 추세 이탈 시 MA20 수준에서 우선 청산.

### 🔧 파라미터 재조정 (`user_config.json`)

| 항목 | v2.x | v3.0 | 의도 |
|------|------|------|------|
| `partial_tp_ratio` | 0.4 | **0.33** | 1차 익절 비중 축소 → 추세를 더 길게 추적 |
| `trailing_stop_activation_pct` | 6.0 | **5.0** | 1x 기준값(레버리지는 ×N 스케일) |
| `breakeven_trigger_pct_us` | 4.0 | **3.0** | 1x 기준값(레버리지는 ×N 스케일) |
| `atr_stop_multiplier` | 2.5 | **2.0** | 손절폭 과도 확장 억제 |
| `correlation_max_per_group` | 3 | **2** | 동조 손실 방지(기획 원안 복귀) |
| (신규) `leveraged_scaling_enabled` | — | **true** | 비대칭 스케일링 on/off |
| (신규) `trend_exit_*` | — | **추가** | 추세붕괴 손절 파라미터 |
| (신규) `portfolio_selection` | — | **추가** | 카테고리별 선정 개수/임계 설정화 |

### 🧪 테스트

- 신규 `tests/test_v30_strategy.py` 12개 (레버리지 스케일링·ATR cap·추세붕괴 게이트·복합 점수).
- 기존에 깨져 있던 weight-sizing 7건 + correlation-cap 1건 정상화.
- 전체 **56개 테스트 통과**.

### ❌ v3.0 비목표 (의도적 보류)
- 신규 LLM/증권사 추가 (기존 안정화 우선)
- ML 기반 신호 (거래 데이터 축적 후 검토)
- 인트라데이 고빈도 매매 (일봉 추세추종 기조 유지)


---

## 📋 v3.1 긴급 복구 및 보안 강화 (2026-07-07)

### 🔴 문제 진단
- 2026-06-24 v3.0 배포 이후 **13일간 봇이 죽어있었음** (프로세스 미존재)
- 대시보드만 살아있어 캐시 갱신은 됐지만 **실제 매매 0건**
- 전 포지션 -6% ~ -19% 손실 고착

### 🛡️ 보안 강화 (CRITICAL)
- **대시보드 인증 추가** (`web/auth.py`): API 키 기반 접근 제어
  - Bearer 토큰 / 쿼리파라미터 / 쿠키 인증 지원
  - 5회/분 Rate Limiting (brute-force 방지)
  - `/api/status`만 비인증 헬스체크 허용 (민감정보 제외)
- `DASHBOARD_API_KEY` 환경변수 추가 (미설정 시 랜덤 키 자동 생성)

### 🔧 봇 안정성 개선
- `market_scanner` 모듈 임포트 실패 시 봇 전체 크래시 → **방어적 임포트 적용**
  - 원인: KIS 토큰 갱신 실패 시 모듈 레벨 `scanner = MarketScanner()` 에서 Exception → ImportError
  - 수정: try/except로 감싸 scanner=None 허용, run_scanner()에서 None 체크
- `MarketScanner()` 생성자 자체도 방어적으로 변경

### 📉 전략 보수화 (손실 최소화 목표)
| 변경 | Before | After | 이유 |
|------|--------|-------|------|
| persona | aggressive | **neutral** | 공격적 매수 지양 |
| daily_investment_pct | 35% | **20%** | 한 세션 노출 축소 |
| max_buys_per_session | 6 | **3** | 과매수 방지 |
| stop_loss_pct | -6% | **-4%** | 빠른 손절 |
| min_successful_llms | 1 | **2** | AI 합의 강화 |
| required_buy_ratio | 0.5 | **0.6** | 매수 기준 엄격화 |
| rebound_buy_enabled | true | **false** | 하락장 반등매수 비활성 |
| pullback_rebuy_enabled | true | **false** | 풀백 재매수 비활성 |
| max_kr_stock | 4 | **0** | 개별주 전면 중단 |
| max_us_stock | 4 | **0** | 개별주 전면 중단 |
| max_us_3x | 3 | **2** | 레버리지 노출 축소 |
| min_etf_momentum | -5.0 | **0.0** | 하락추세 ETF 진입 차단 |

### 📊 포트폴리오 리밸런싱
- 개별주(KR 20종 + US 15종) 후보 풀에서 **전면 제거**
- ETF만 유지: KR 7종(코어) + US 1X 3종 + US 3X 2종
- 모멘텀 양수(상승추세) 종목만 신규 편입 허용
